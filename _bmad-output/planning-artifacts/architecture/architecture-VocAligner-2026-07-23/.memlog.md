---
scope: VocAligner MVP
purpose: Architecture spine for VocAligner's AI vocal-chain generation product, brownfield (Milestone 4 in progress)
altitude: initiative-to-features (whole project, above epics)
updated: 2026-07-24T16:48
---

- (direction) User delegated working mode: skip interactive coaching for this run, proceed pragmatically; beginner-guidance need is now handled durably via CLAUDE.md/project-context.md instead of per-session hand-holding
- (decision) PRD (prd-VocAligner-2026-07-23) is the primary input; brownfield codebase investigated directly (lib/ai, lib/schema, lib/registry, lib/validation, app/) to ratify existing conventions rather than invent new ones
- (decision) AD-1 [ADOPTED]: Pipeline stages omit app-derived bookkeeping fields (order, wasRepaired, intent id) from the model's request schema; orchestrator computes them post-call. Binds lib/ai/stages/*, generateVocalChain.ts
- (decision) AD-2 [ADOPTED]: ModelClient interface is the only AI-provider boundary; only *ModelClient.ts files may import a provider SDK. Prevents bypassing retry/observability/error-normalization
- (decision) AD-3 [ADOPTED]: validateAndRepairChain classifies valid/repaired(clamp)/rejected(unknown plugin-or-control->retry); only valid/repaired chains ever leave generateVocalChain
- (decision) AD-4 [ADOPTED]: Zod schema is single source of truth; TS types always via z.infer, never hand-declared separately; module public surface via index.ts barrel
- (decision) AD-5 [ADOPTED]: pluginRegistry (keyed by daw+tier) is the closed world constraining generation/validation; Daw is currently a closed enum ['logic-pro'] - adding a DAW is a deliberate schema+registry change, not runtime-open
- (decision) AD-6 [ADOPTED]: design tokens centralized as CSS custom properties in globals.css via Tailwind @theme inline; components consume only semantic classes, never raw hex
- (decision) AD-7 (new, resolves PRD open questions 1 and 2): Cache is global (no per-user concept exists in MVP data model anyway - no auth), keyed by normalized (trim+lowercase) Artist+Song text, no fuzzy matching in MVP
- (decision) AD-8 (new): cache/versioning tie-in - cache lookup must match on PIPELINE_VERSION + PROMPT_VERSION in addition to the artist+song key, so a structural or prompt change invalidates stale cached chains rather than serving them under a new pipeline
- (decision) AD-9 (new, formalizes existing CLAUDE.md/ARCHITECTURE.md rule as an enforceable dependency rule): only app/api/generate/route.ts may import lib/ai/*; pages/components reach generation only over HTTP, may import lib/registry and lib/schema directly
- (assumption) Paradigm named as: pipeline (pipes-and-filters) core for the AI generation flow, Ports & Adapters at the ModelClient boundary, conventional Next.js App Router layering for delivery
- (question) schema/chain.ts controlValueSchema already has a per-control confidence enum (low/medium/high) fully unused in the UI - code is ahead of the PRD here (PRD Confidence Score marked not-yet-designed/future). Worth surfacing to founder: may be cheaper to expose than PRD assumed.
- (question) domain/registry.ts pluginTierSchema already includes free-3rd-party and commercial tiers though product policy (PRD Non-Goals) is stock-only permanently. Flagged as Deferred rather than silently trimmed or endorsed - genuine judgment call.
- (version) Stack as pinned in web/package.json: Next.js 16.2.10, React/react-dom 19.2.4, TypeScript 5, Zod 4.4.3, @anthropic-ai/sdk 0.110.0, Tailwind CSS 4, Vitest 4.1.10, ESLint 9 - treated as ratified brownfield reality (ADOPTED)
- (event) Investigation complete, drafting spine now
- (event) Reviewer gate ran: lint_spine.py clean; rubric walker (2 critical, 1 high, 2 medium, 2 low), version-verification lens (stack all current/coherent, flagged non-blocking: hardcoded Anthropic model id claude-sonnet-4-5-20250929 is superseded, revisit before live cutover), adversarial lens (6 genuine divergence holes)
- (decision) Fix CRITICAL-1: AD-4 barrel-only-import clause contradicted brownfield reality (near-universal deep imports, lib/ai barrel missing exports its real consumer needs) - dropped the barrel clause, kept only the schema-single-source-of-truth half
- (decision) Fix CRITICAL-2 / Adversarial Hole 1+2 / MEDIUM-2: added AD-10 - cross-page result handoff is by opaque id only (never full payload in URL or an ad hoc client store); cache stores the whole VocalChainResponse; a cache hit replays it unmodified except cacheHit=true, preserving original id/generatedAt; this also fixes cache placement as an enforceable Rule
- (decision) Fix HIGH-1: added getModelClient.ts to Structural Seed's lib/ai listing, noted as the mock/live switch point
- (decision) Fix Adversarial Hole 3: tightened AD-5 - every entry in logicPro.ts must have tier:'stock'; other PluginTier values reserved for a future, deliberately-scoped DAW/tier expansion
- (decision) Fix Adversarial Hole 4: scoped AD-3 explicitly to what's actually implemented (numeric range clamp + plugin/control existence check); string/boolean control value validation doesn't exist yet - moved to Deferred rather than speculatively speccing an exhaustive table for an unbuilt feature
- (decision) Fix Adversarial Hole 5: corrected AD-1 wording to match reality (a stage computes its own bookkeeping field immediately, not a separate orchestrator pass); bound repairChain.ts into AD-1 and added a rule that any future code changing plugins[] cardinality must renumber order in the same place, before returning
- (decision) Fix Adversarial Hole 6 / LOW-1: tightened AD-6 to ban any raw Tailwind palette utility (not just hex) outside the --color-* token set, and defined the token-addition process; flagged existing raw-utility usage in page.tsx/results/page.tsx/loading/page.tsx as a Deferred cleanup item rather than refactoring it now (out of scope for the architecture pass)
- (decision) Fix MEDIUM-1: noted in Consistency Conventions that ObserveStage is plumbed into every stage but not yet consumed anywhere (generateVocalChain.ts passes no observe callback) - added to Deferred as 'wire a consumer (logging/metrics)' rather than inventing one now
- (assumption) Reconciliation-against-PRD step satisfied by the rubric reviewer's explicit FR-1..FR-7 coverage check (PASS) rather than a separate reconciliation subagent pass, given time/cost tradeoff and the thoroughness already achieved
- (question) Version-verification lens flagged (non-blocking): anthropicModelClient.ts hardcodes DEFAULT_MODEL='claude-sonnet-4-5-20250929', an active but superseded model id. Not an architectural issue, but worth a conscious swap to a current model before the live-Anthropic cutover ships. Added to Deferred.
- (event) spine finalized
- (event) Post-finalize addition: logged Plugin Variant Selection as a Deferred item (extends AD-5's registry pattern - variants + authored character descriptions, same as existing education field). Confirmed future, not started, no schema support yet. Founder explicitly parked rather than folding into MVP.
- (event) Refresh 2026-07-24: docs/DESIGN_SYSTEM.md updated to v1.1 (founder converged on landing page direction: DesignH hero + dark 'Meet VocAligner' how-it-works section, still living in /design-preview sandbox pending promotion to real page.tsx). Updated AD-6 to cover the gradient-via-inline-style pattern (multi-stop gradients aren't expressible as Tailwind utilities) and the accent/muted token-name collision; added shadcn/motion/tw-animate-css etc to Stack table. Confirmed functional pages (loading/results) intentionally keep the old simple gradient - not affected by this change.
