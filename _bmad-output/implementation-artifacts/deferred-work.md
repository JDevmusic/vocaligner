# Deferred Work

## Deferred from: code review of 1-1-promote-landing-design (2026-08-01)

- Untrimmed `artist`/`song` state reaches the `/loading` query string even though `canGenerate` gates on trimmed length (`web/app/page.tsx:23,29`) — pre-existing behavior faithfully ported from the old `page.tsx` (Story 1.1's Dev Notes explicitly instructed porting the old submission logic rather than reinventing it), not a regression introduced by this story. Likely low real-world impact since AD-7's cache normalizes on trim+lowercase downstream, but the client-sent value itself is still untrimmed. Small fix whenever `page.tsx` is next touched: `.trim()` both fields when building `URLSearchParams`.
- No re-entrancy guard against rapid double-submit calling `router.push` twice (`web/app/page.tsx:25-31`) — also pre-existing in the old `page.tsx`, ported unchanged. Low impact (duplicate client-side navigation to the same URL is harmless), not blocking.
- Fixed `size={12}`/`size={16}` on the hero's artist/song text inputs with no `maxLength` (`web/app/page.tsx:106,117`) — new in this story's sentence-style layout (carried from the design-preview mockup). A long artist name or song title scrolls within a fixed-width slot rather than growing or wrapping. Not tied to a numbered AC; worth a small UX pass later (e.g. a sensible `maxLength` plus a slightly more forgiving width).
- Stale comment in `web/app/globals.css` (~line 131): "*Scope is results only -- loading/landing still use hero-gradient.*" is now inaccurate since Story 1.1 replaced the landing page's `.hero-gradient` usage with a hand-rolled inline gradient. Note: this comment was actually introduced by a later, unrelated commit (`a86b9db`, 2026-07-31) — not part of Story 1.1's diff at all — so it isn't this story's fault, but it's a live inaccuracy in the current codebase worth fixing whenever `globals.css` is next touched.

## Deferred from: code review of 1-2-real-generation-request (2026-08-01)

- No client-side timeout/`AbortController` on the `/loading` page's `/api/generate` fetch (`web/app/loading/page.tsx:45-73`) — AC4 explicitly lists "times out" as a failure mode requiring an explicit failure state, but the current implementation only handles non-2xx responses and thrown/network errors; a request that never resolves leaves the loading page frozen at 100% forever. Zero live risk today because `getModelClient()` only ever returns the deterministic, fast mock client — the real Anthropic adapter isn't wired in yet (per project context). Add an `AbortController`-based timeout (duration TBD, likely tied to whatever real-world latency budget the real adapter ends up needing) as part of whichever story wires in the real Anthropic client (Phase 4a), rather than reopening this story now.
- `web/lib/store/generationStore.test.ts` imports `lib/ai/generateVocalChain` and `lib/ai/mockModelClient` directly, in literal (not spirit) tension with Architecture AD-9's wording ("only `app/api/generate/route.ts` may import from `lib/ai/*`"). Deliberate choice per this story's Dev Agent Record (avoids a fragile hand-fabricated fixture that could drift from the real nested schemas) and poses no real risk since test code never reaches the client bundle. Worth a small wording tweak to AD-9 in a future architecture-doc pass to explicitly exempt test files, rather than a code change.
- React Strict Mode (on by default for the App Router since Next.js 13.5.1, unconfigured/default in this project) double-invokes `loading/page.tsx`'s fetch effect in development, firing two real `/api/generate` POSTs per page load in dev mode only. Production builds don't double-invoke, so there's no user-facing impact today. Once the real Anthropic adapter replaces the mock client, this will double local dev-testing cost/latency per page load — worth adding an idempotency guard (e.g. a ref-based "already fired" flag) at that point.

## Deferred from: code review of 1-3-plugin-visual-component (2026-08-02)

- `invert` prop on `NumberArcKnob` (`web/app/components/controls/PluginKnobPrimitives.tsx`) is dead/untested code — built specifically for DeEsser 2's Max Reduction dial, but that component bypasses it via a hand-computed `ArcKnob` + `invertedKnobRotationDeg(...)` call instead. Zero real-world coverage of the `invert` branch (including its min/max label-swap logic). Either wire DeEsser 2's Max Reduction through `NumberArcKnob { invert }` for real coverage, or remove the unused prop.
- `channelEqCurve.ts`'s `GAIN_TICKS`/`GAIN_LABELED` (`web/lib/eq/channelEqCurve.ts:242-243`) include values down to -60dB, but `dbToY()` is linear only across ±15dB onto the plot's actual pixel span — any tick below -15dB computes a y-coordinate far outside the SVG viewBox and never renders. Leftover dead constants from the removed "two-slope" axis model. Trim both sets to the real ±15dB range whenever this file is next touched.
- `ChannelEqVisual.tsx` and `PitchCorrectionVisual.tsx` each hand-roll their own card+header markup (border/rounded-corner/shadow/header structure) instead of using the shared `PluginPanel` from `PluginKnobPrimitives.tsx`, which the other 8 knob-based visuals all use — three near-identical copies of the same chrome, against the project's own "don't duplicate UI" principle. Checked how close the fit actually is before deferring further (2026-08-02): Channel EQ maps onto `PluginPanel` almost exactly (`width: "1000px"` only, header `px-6 py-4` — identical to `PluginPanel`'s hardcoded default), genuinely low-risk to migrate. Pitch Correction does not: its header is `px-6 py-3`, one size down from `PluginPanel`'s hardcoded `py-4`, and Pitch Correction is the component with the deepest pixel-QA history of all 10 (7 sub-rounds, see its Dev Agent Record entries). A mechanical migration would either silently regress that verified spacing or require first adding a header-padding override prop to `PluginPanel` (and confirming py-3 is actually still correct against the reference, not just an old inconsistency) — not a drive-by change. Channel EQ's half could reasonably be done alone; Pitch Correction's needs its own dedicated pass with re-verification.
- `formatKnobValue`'s per-parameter display overrides (`"ratio"`, `"drive"`, `"output"`, `"rate"`, `"intensity"`, `"decay"`) are matched by bare parameter name with no plugin scoping, shared globally across all 8 knob-based plugins. A future plugin registering a control with one of these exact names but a different real display convention would silently inherit the wrong formatting. No current collision exists. Consider scoping by `(pluginId, parameter)` if/when a collision actually surfaces.
- Three parallel log-scale implementations exist with different signatures/defaults: `logFraction` in `PluginKnobPrimitives.tsx` (hardcoded `1e-6` epsilon), a second `logFraction` in `PhaserVisual.tsx` (configurable epsilon, needed because Rate 1/2's 0-10Hz range needs a much bigger floor than `1e-6` provides), and the angle-domain `logKnobRotationDeg` in `knobRotation.ts`. A fix to the underlying log-scale-with-floor approach in one won't propagate to the others. Worth consolidating into one shared helper if a fourth use case ever needs the same idea.
- `resolveChannelEqBands`'s `resolveNumber` helper (`web/lib/eq/channelEqCurve.ts:51-57`) defaults an unresolved control value (including Q) to 0 if the registry definition itself somehow lacks a numeric default — a resolved `q=0` divides by zero in `biquadCoeffs`'s `alpha = sinw0/(2*q)`, propagating NaN through that band's whole curve response. Not currently reachable: every Channel EQ band's Q has a real registry default, and any live-Generation value is clamped to the control's registered min/max by `validateAndRepairChain` before this component ever sees it (this project's documented convention: "callers never re-validate `ModelClient` output"). Flagging because the component itself has no independent defense — relevant if this component is ever driven by hand-written data that bypasses the validation pipeline (e.g. a future test).
- The 24dB/Oct cut-band model (`web/lib/eq/channelEqCurve.ts`'s `cutBandResponseDb`) cascades two identical 12dB/Oct biquad stages at the same Q, rather than the differing per-stage Qs a real 4-pole (e.g. Butterworth) filter design would use. Internally consistent with its own tests (correctly doubles the dB at any given frequency far from cutoff) and a deliberate, already-documented choice in this story's own Dev Notes — but the curve's exact shape right at the cutoff knee won't perfectly match Logic's real 24dB/Oct response. Revisit if a future pixel-comparison against the reference ever specifically targets Band 1/8's cutoff knee shape.
- `niceTickValues` (`web/lib/controls/knobTicks.ts`) rounds every generated tick to 3 decimal places — fine for every control range currently in the registry, but would silently collapse distinct tick values to the same number for any future control registered with a sub-0.001-wide range. No current control is affected; nothing in the type system prevents one from being added later without revisiting this.
- `toneCurveY` (`web/lib/eq/overdriveResponseCurve.ts`) only clamps its lower bound (`Math.max(-1, db / TONE_DB_SPAN)`) despite its own comment describing `TONE_DB_SPAN` as the clamp span for the full normalized `[-1, 1]` range. Currently inert — a lowpass filter response never exceeds ~0dB passband gain — but the upper-bound guarantee the comment claims isn't actually enforced in code. Add the matching `Math.min(1, ...)` for defense-in-depth whenever this file is next touched.
- The bipolar knob fill's "zero" position (`web/app/components/controls/PluginKnobPrimitives.tsx`, `arcKnobRotationDeg((min + max) / 2, min, max)`) assumes the control's range is symmetric around zero. True for every bipolar control actually wired up today (Flanger's Feedback -100..100, Tape Delay's Clip Threshold/Spread, both symmetric) but not enforced — a hypothetical future asymmetric bipolar range (e.g. -10 to 30) would silently start its fill from the arithmetic midpoint instead of the true zero point.
- DeEsser 2's PRD §8.9 rule (Threshold/Max Reduction/Frequency always render at Logic's own defaults, never real per-song generated values) is enforced only by the fact that `DeEsser2Visual`'s function body never reads its declared `values: ControlValue[]` prop — confirmed correct by reading the full file, but nothing beyond a code comment stops a future edit from "fixing" this to read live data like every sibling knob-based component does, re-introducing the exact false-precision problem PRD §8.9 exists to prevent. Consider a type-level or lint-level guard if this component is touched again.
- `numericTicks`' `exclude` option (`web/app/components/controls/PluginKnobPrimitives.tsx`) filters via exact value equality (`options.exclude!.includes(v)`) rather than a floating-point tolerance. Currently safe — the only caller, Compressor's Make Up dial, excludes a clean integer (`40`) that `niceTickValues` also generates as an exact integer — but would silently fail to exclude a tick if a future caller's excluded value doesn't exactly match the generated float.

## Deferred from: code review of 1-4-results-page-real-chain (2026-08-02)

- In-memory `generationStore` (module-level `Map`, `web/lib/store/generationStore.ts`) won't survive a `GET /api/generate/[id]` landing on a different server instance than the `POST /api/generate` that saved the generation (serverless/edge/multi-worker deploys) — a just-created generation could 404 on the very next request. Pre-existing from Story 1.2, whose own Dev Notes explicitly authorize "in-memory is correct for now" as an architecture-level deferred decision; Story 1.4 only depends on it (its new `GET` route reads from the same store), it doesn't introduce the limitation. Revisit alongside whichever future story adds real persistence.
- Same store has no TTL or eviction path — every successful generation is added and never removed, so memory grows for the lifetime of the process. Same pre-existing Story 1.2 architecture decision as above.
- No `aria-live`/`role="status"` announcement on `results/page.tsx` or `loading/page.tsx` for loading/error state transitions — both use plain `<p>` elements, so a screen reader won't proactively announce "loading" → "ready"/"error" transitions. Matches the existing pattern already present on `loading/page.tsx` since Story 1.2, not newly introduced by this story, and no design-system/architecture doc currently mandates it. Worth revisiting as a small accessibility pass once the app's a11y bar is explicitly defined.
- An empty `chain.plugins` array has no dedicated handling in `results/page.tsx` — the page would render a "successful"-looking page with the heading/artist line but no plugin cards and no messaging. Nothing in `chainSchema`/`pluginInstanceSchema` (`web/lib/schema/chain.ts`) guarantees non-emptiness, so this is really an upstream generation-pipeline invariant question (should the pipeline guarantee at least one plugin, or should the *page* guard against zero), outside this story's fetch/dispatch-wiring scope.
- Task 4's typography/spacing consistency pass was only partially applied to `loading/page.tsx` — the `<Link>` → `AnimatedButton` swap happened, but no heading/body-text sizing changes were made. Soft finding: the page has no true heading element to begin with (only a `text-lg font-medium ... sm:text-xl` phase-status line, already reasonably close to the landing page's body scale), so the practical gap is small. Worth a quick look if `loading/page.tsx` is touched again for an unrelated reason.

## Deferred from: code review of 2-1-serve-cached-results (2026-08-02)

- Two near-simultaneous first-time requests for the same new Artist+Song both miss the cache and both call the (paid) model client — real and plausible (e.g. a double-click on "Generate"), but explicitly called out as out of scope in Story 2.1's own Dev Notes, and no concurrency control exists anywhere in `generationStore.ts` since Story 1.2. Revisit with an in-flight-request de-duplication mechanism if duplicate model calls prove costly in practice.
- AD-8's version-mismatch branch (`getCachedGeneration` returning `undefined` on a `pipelineVersion`/`promptVersion` mismatch) has no reachable path in real usage today — both are compile-time constants and the cache is a bare in-memory `Map` wiped on every process restart, so a stale-version entry can never coexist with the current constants inside one running process. Only exercised by hand-forged test records; becomes a real path once cache storage moves off in-memory, which is already an explicit open/deferred architecture decision (`ARCHITECTURE-SPINE.md`'s "Cache storage technology" item).
- The Artist+Song cache (`web/lib/store/generationStore.ts`'s new `cachedByKey` Map) is entirely unbounded — no TTL, no LRU, no max entry count — same pre-existing posture as the original id-keyed `Map` since Story 1.2, and "cache storage technology" is already flagged as deferred in the Architecture Spine. Story 2.1 doubles the growth rate (two Maps instead of one) but doesn't introduce the pattern.
- No test-only reset utility for the module-level store — cross-test isolation currently relies on every test using a unique Artist+Song pair (a human-memory convention, not tooling-enforced). A `resetGenerationStore()` export would be more robust as the test suite grows and more files start exercising the cache.
- AC1 ("no call is made to the model client") and AC3 (version-mismatch → miss) are proven only indirectly in the test suite — matching `id`/`generatedAt` across two requests for AC1, and a `generationStore`-level unit test with a hand-forged stale entry for AC3 — rather than a direct spy on `getModelClient` or a route-level integration test for the version-mismatch path. Reasonable proxies given the current control flow (a fresh generation necessarily produces a new `randomUUID()`/timestamp), worth strengthening if this path grows more complex.
- Whitespace-only artist/song (e.g. a single space) passes `vocalChainInputSchema`'s `.min(1)` then normalizes to an empty string in `normalizeCacheKey`, so distinct "blank-ish" inputs alias onto the same cache entry. Pre-existing input-validation gap from Story 1.1's schema (which only checks length, not content), not introduced by this story.

## Deferred from: code review of 3-1-switch-to-real-anthropic-client (2026-08-02)

- `ANTHROPIC_API_KEY` truthiness/non-whitespace check (`web/lib/ai/getModelClient.ts`) has no format/sanity validation beyond presence — a garbage, truncated, or half-copied key still routes to the live Anthropic client and only fails opaquely later, deep inside a `generateStructured()` call, rather than with a clear error at startup/config time. Real, but a bigger scope (API-key format validation) than the switch this story added; worth a small follow-up.
- No observability into which mode (mock vs. live) actually served a given request — a misspelled env var name or a `.env.local` that didn't get picked up would silently keep serving mock responses with zero indication. `ObserveStage` (already plumbed into every stage but with no wired consumer, per `ARCHITECTURE-SPINE.md`'s own Deferred section) would be the natural place to add a `mode: "mock" | "live"` signal once observability is actually wired up.
- No validation that a custom `ANTHROPIC_MODEL` override (documented in `.env.example` as a supported escape hatch) actually supports Anthropic's strict tool use — strict-tool-use support is model-specific, not universal across every Claude model. `DEFAULT_MODEL` itself (`claude-sonnet-5`) is confirmed correct and does support it; this only matters for an operator who overrides it to an incompatible model, which would silently reintroduce the exact `ModelResponseValidationError` risk this story's `strict: true` follow-up fix closed for the default case.
- Pre-existing gaps in `anthropicModelClient.ts`'s `callWithSchema`/`isRetryableError`, unrelated to this story's actual changes: no distinct handling for a truncated tool call (`stop_reason: "max_tokens"`) vs. a genuinely missing `tool_use` block — both currently throw the same generic `ModelResponseValidationError` with the same message; the retryable-error check (`Anthropic.APIConnectionError | RateLimitError | InternalServerError`) doesn't explicitly cover an "overloaded" (529) response; no assertion that the schema passed to `callWithSchema` is object-rooted before building `input_schema`. None introduced or worsened by this story; candidates for a future adapter resilience pass.
- `web/.gitignore`'s `!.env.example` negation was verified narrowly (one `git check-ignore -v` case on this machine's repo state) — a contributor with a global `core.excludesfile` that also matches `.env*` could still have the file silently excluded regardless of this repo's own `.gitignore`. Informational; no code fix applies, just a heads-up if a future contributor reports `.env.example` mysteriously not showing up in `git status`.

## Found via live testing against the real Anthropic API, post-3.1 (2026-08-02) — founder flagged as should-fix-sooner, not standard-priority backlog

- ~~**Confirmed real: an empty (or near-empty) generation result is silently accepted as `"status": "valid"`...**~~ **RESOLVED by Story 3.2** ("Guarantee a minimum-quality reasoning and generation result", commit `22f9f15`, status: done — see `_bmad-output/implementation-artifacts/3-2-guarantee-minimum-quality-result.md`). `reasoningSchema.processingIntents` now has `.min(3)` (`MIN_PROCESSING_INTENTS`, enforced as an application-level check since Anthropic's strict tool mode caps wire-facing `minItems` at 1), `chainSchema.plugins` has `.min(1)`, and both the Reasoning and Generation stages retry (`MAX_REASONING_ATTEMPTS`/`MAX_GENERATION_ATTEMPTS = 2`) on a too-thin result instead of accepting it — confirmed live in current code (`web/lib/schema/reasoning.ts`, `web/lib/schema/chain.ts`, `web/lib/ai/generateVocalChain.ts`) with dedicated test coverage (`generateVocalChain.test.ts`). This entry was left stale here after the fix shipped — worth checking a deferred-work.md entry against current code before assuming it's still open, same lesson as elsewhere in this project.

## Flagged by founder during model-comparison session (2026-08-05) — product/architecture question, not yet scoped

- **Insert vs. send/bus routing isn't represented anywhere in the product.** Real Logic Pro engineers frequently route time-based effects (reverb, delay) to a send/Aux bus for parallel processing, rather than placing them as a channel insert — but `chainSchema`/`pluginInstanceSchema` (`web/lib/schema/chain.ts`) only models a flat, ordered list of inserts; there's no concept of a bus, a send level, or parallel (vs. serial) signal flow anywhere in the registry or schema. Concretely relevant to 2 of the 10 registry plugins — ChromaVerb and Tape Delay, both currently modelled purely as inserts with a Dry/Wet knob — essentially irrelevant to the other 8 (Compressor/EQ/De-Esser/Overdrive/Chorus/Flanger/Phaser/Pitch Correction are all conventionally inserts in real practice regardless). The classic reason to use a bus (sharing one reverb across many tracks in a full session) doesn't directly apply since VocAligner only models a single vocal channel today, not a multitrack session — but send-based routing still produces a genuinely different result than an insert's internal Dry/Wet crossfade, and matching what a real engineer would actually do is the product's whole premise. Open question, not yet decided: represent this as a real schema concept (an `insert`/`send` routing type per plugin instance) vs. just descriptive guidance text on the ChromaVerb/Tape Delay visuals ("route this via a send, not an insert") without a schema change. Needs a dedicated scoping pass, not a drive-by fix.

## Flagged by founder during live Logic Pro A/B testing, Anthropic vs. Luna (2026-08-07) — visual/data accuracy fixes, not yet scoped

- **ChromaVerb's Dry/Wet faders are still clipped even after the 2026-08-03 width fix (1000px → 1080px, `ChromaVerbVisual.tsx`).** The fix is still present in the code — not a regression — but the founder confirmed live the faders are still missing. Most likely the hand-calculated safety margin (computed from summing padding/knob widths in the source, without a real browser to verify) was still insufficient. Needs an actual pixel measurement next time, not another blind estimate — consider a much larger margin or a structurally different fix (e.g. don't rely on a fixed px width at all) rather than incrementing the same number again.
- **ChromaVerb's Decay knob angle doesn't match real Logic at low values.** `logKnobRotationDeg` (`web/lib/controls/knobRotation.ts:49-65`) is calibrated from a single real reference point (Decay = 1.10s → needle at 39% of the sweep, from `docs/images/reference/Chromaverb_plugin.png`) with `floor=0.06` solved backward from just that one value — the function's own comment already flags this as needing a second real data point to refit properly. Founder tested live in Logic at Decay = 0.6s and reports the real needle sits "a lot closer to the start" than the app currently renders (current curve predicts ~31% at 0.6s, vs. ~39% at 1.1s) — confirms the single-point calibration doesn't hold, but "a lot closer to the start" isn't precise enough to safely refit blind. Get an exact reading (clock-position or a photo) next time before attempting the refit.
- **Channel EQ's Q value never gets the bold/brand-accent treatment real (non-default) data gets elsewhere on the same card.** ~~`ChannelEqVisual.tsx:168`~~ **Fixed 2026-08-11**: Q now uses the same `band.enabled` flag Freq/Gain already used, no new tracking needed.
- **ChromaVerb Dry/Wet, second attempt.** Widened further to 1250px (2026-08-11), a deliberately large safety margin rather than another marginal guess, after 1080px was confirmed still insufficient. Awaiting founder confirmation this actually resolves it.
- **Generated numeric values may not land on anything actually selectable in Logic's real UI, across all plugins/controls.** Founder flagged (2026-08-11): the app generates continuous numeric values (e.g. Decay = 0.6s), but real Logic knobs may snap to discrete steps or have limited practical precision, so a generated value isn't guaranteed to be exactly dial-able in the real plugin. Explicitly flagged as systemic (potentially every control on every plugin, not just one) and explicitly deferred by the founder as not essential right now — would need a full per-control audit of Logic's actual real-world step precision before any fix could even be scoped, let alone built. Founder is logging concrete examples as they're found during live Logic testing, to build real evidence before this is ever scoped:
  - Channel EQ, band at 1100 Hz: generated Q = 0.80, nearest value actually selectable in real Logic is 0.79.
  - Channel EQ, band at 320 Hz: Q value also reported as not landing exactly — specific target/actual numbers not yet captured, follow up next time this comes up.
  - Compressor: generated Ratio = 3:1, not selectable in real Logic — nearest values are 2.9:1 or 3.1:1.
  - Compressor: generated Attack = 25 (ms), nearest value actually selectable in real Logic is 26.
  - Chorus: Rate's real Logic step size is 0.033 Hz increments (a step size, not a single mismatched pair like the examples above — useful since it lets any generated Rate value be rounded to the nearest real step directly, rather than needing a full lookup table).
- **Phaser's `sweepMode` (`logicPro.ts:253`) has the exact same shape of problem Pitch Correction's rootNote/scale had before the 2026-08-11 `options` fix** — a free-text `type: "string"` control with no constraint tying it to Logic's real, finite Sweep Mode dropdown. Not fixed alongside rootNote/scale because, unlike those two (fully transcribed and verified in `scaleIntervals.ts` against `docs/plugin-references.md` and Apple's own docs), there's no verified real option list for Phaser's Sweep Mode anywhere in this project's docs yet — fixing it now would mean guessing at the real dropdown values, the exact mistake the `options` mechanism exists to prevent. Needs the real option list confirmed (reference screenshot or docs) before the same fix can be applied.
- **Tape Delay's Time knob — two distinct real issues found together (2026-08-11), not just a rounding mismatch like the list above:**
  1. **Registry range looks wrong.** `logicPro.ts:151` has Time as `min: 1, max: 2000` (ms), but founder reports real Logic's actual range runs from 85.5 to 4999.9 ms — both the floor and the ceiling are off, not just imprecise.
  2. **Knob likely needs log scaling, same class of bug as ChromaVerb's Decay knob (see above).** `TapeDelayVisual.tsx:100` renders Time via plain `<NumberArcKnob plugin={plugin} values={values} parameter="time" size={DELAY_TIME_SIZE} />` with no `log` prop — a linear mapping. Founder reports the real dial's scale visibly compresses near the high end ("the scale seems to change as it gets closer to the end value"), causing the rendered needle to point in the wrong direction for a given value — the same symptom ChromaVerb's Decay had before that was traced to needing a log curve. Fix direction: correct the registry range first (needs a real confirmed min/max, not just founder's approximate reading), then add `log` (and likely a fitted `logFloor`, same approach as Decay) once real reference values are available at a couple of different points across the range to calibrate against — don't guess the curve blind, same lesson as the Decay entry above.

## Deferred from: code review of the landing page hero redesign, commit 4ffdb8b (2026-08-22)

- **"See For Yourself" renders as a plain `<span>`, not a heading** (`web/app/page.tsx`) — functions as the CTA section's label but isn't in the document's heading hierarchy, so assistive tech gets no landmark for it. Low impact (the real `<h1>` above it is intact and unique), worth an `h2`/`h3` swap whenever this block is next touched.
- **The Artist and Song field blocks in the hero form are near-identical ~15-line copies** (`web/app/page.tsx`) — a small shared `LabeledInput` component would fit CLAUDE.md's "don't duplicate UI" guidance. Judgment call at only 2 instances; not extracted now since a third near-identical use isn't in sight yet.
- **No `maxLength`/truncation handling on the Artist/Song inputs** — a long name just scrolls inside the (now-responsive) box with no ellipsis or overflow cue. Same pre-existing gap noted for the original Story 1.1 inputs, still true after this redesign.
- **Gold Generate button's contrast against `--hero-panel` is low by raw luminance (~1.6:1)**, though the button text itself (near-black on gold) measures a strong ~9.5:1 and the button reads as distinct in practice via hue difference + drop shadow. Confirmed via computed contrast math during review, not yet via an actual screenshot — worth a quick visual eyeball check next time the hero is touched, rather than trusting the math alone.
- (Not re-logged — already tracked above under the 2026-08-01 entry: `handleGenerate` still forwards untrimmed `artist`/`song` into the query string despite `canGenerate` trimming for validation. Confirmed still present after this redesign, same pre-existing gap, no new fix needed here.)
